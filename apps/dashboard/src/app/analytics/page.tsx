import { ComingSoon } from '@/components/ComingSoon';

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      reason="Message delivery analytics land in M12. No analytics endpoint exists on apps/api yet."
    />
  );
}
